import { VantButton, VantButtonEvents, VantButtonExternalClassName, VantButtonProps } from 'vant-weapp-typings';

type Button = React.ComponentType<VantButton>;

export { VantButton, VantButtonEvents, VantButtonExternalClassName, VantButtonProps };

declare const Button: Button;

export default Button;
