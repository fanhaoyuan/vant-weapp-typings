import { VantField, VantFieldEvents, VantFieldExternalClassName, VantFieldProps } from 'vant-weapp-typings';

type Field = React.ComponentType<VantField>;

export { VantField, VantFieldEvents, VantFieldExternalClassName, VantFieldProps };

declare const Field: Field;

export default Field;
