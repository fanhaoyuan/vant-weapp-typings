import { VantCard, VantCardProps, VantCardExternalClassName } from 'vant-weapp-typings';

type Card = React.ComponentType<VantCard>;

export { VantCard, VantCardProps, VantCardExternalClassName };

declare const Card: Card;

export default Card;
