import { VantCol, VantColProps, VantColExternalClassName } from 'vant-weapp-typings';

type Col = React.ComponentType<VantCol>;

export { VantCol, VantColProps, VantColExternalClassName };

declare const Col: Col;

export default Col;
